class('Property').extends()

function Property:onPlay(card, game)
    -- Override on child class
end

function Property:onDraw(card, game)
    -- Override on child class
end

function Property:onDiscard(card, game)
    -- Override on child class
end

function Property:onBanish(card, game)
    -- Override on child class
end